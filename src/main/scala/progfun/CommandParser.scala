package progfun

class CommandParser(private val lines: List[String]) {

  //  val getLimits: Limits = lines.headOption.getOrElse("").split(' ') match {
  //    case x, y => Limits(x.toInt, y.toInt)
  //    case _ =>
  //  }
}
